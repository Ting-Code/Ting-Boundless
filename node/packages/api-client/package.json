{
  "name": "@ting/api-client",
  "version": "0.0.0",
  "private": true,
  "description": "Optional orval + TanStack Query hooks over @ting/api-types",
  "scripts": {
    "build": "echo \"Optional; scaffold when needed\" && exit 1"
  },
  "peerDependencies": {
    "@ting/api-types": "workspace:*"
  }
}
